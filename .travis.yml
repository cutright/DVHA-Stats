dist: xenial
language: python
python:
    - 3.5
    - 3.6
    - 3.7
    - 3.8
install:
    - pip install -r requirements.txt
    - pip install coveralls
    - pip install codecov
    - pip install codacy-coverage
services:
  - xvfb
script:
  coverage run --source=DVHA-Stats setup.py test
